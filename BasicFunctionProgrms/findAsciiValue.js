// Write a program to print all the ASCII values and their
// equivalent characters using a while loop. The ASCII values vary from 0 to 255.

let char = 0;

while(char < 256){
    console.log('ASCII VALUE OF '+str+ 'char'.charCodeAt(0))
}